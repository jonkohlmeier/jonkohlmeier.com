---
const year = new Date().getFullYear();
const footerLinks = [
  { href: '/now/', label: 'Now' },
  { href: '/writing/', label: 'Writing' },
  { href: '/services/', label: 'Advisory' },
  { href: '/rss.xml', label: 'RSS' },
];
---

<footer class="border-t border-slate-800/60 bg-slate-950/80">
  <div class="mx-auto flex w-full max-w-6xl flex-col gap-6 px-6 py-10 text-sm text-slate-400 md:flex-row md:items-center md:justify-between">
    <div>
      <p class="font-semibold text-slate-200">© {year} Jonathan Kohlmeier</p>
      <p class="mt-1 max-w-xl leading-relaxed text-slate-400">
        Building thoughtful RevOps systems, writing about leadership and technology, and sharing the
        finer things like pipes, coffee, and books.
      </p>
    </div>
    <div class="flex flex-wrap gap-4 uppercase tracking-[0.3em]">
      {footerLinks.map(({ href, label }) => (
        <a
          href={href}
          class="text-slate-500 transition hover:text-amber-200"
          target={href.startsWith('/') ? undefined : '_blank'}
          rel={href.startsWith('/') ? undefined : 'noopener noreferrer'}
        >
          {label}
        </a>
      ))}
    </div>
  </div>
</footer>
