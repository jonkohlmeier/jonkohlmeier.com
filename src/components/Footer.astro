---
import { SOCIAL_LINKS } from '../data/social';
const year = new Date().getFullYear();
---

<footer class="border-t border-[var(--color-border)] bg-[var(--color-surface)]">
  <div class="container-page flex flex-col gap-8 py-10 md:flex-row md:items-start md:justify-between">
    <div class="max-w-xl space-y-3">
      <p class="brand-title">Puffs of Smoke</p>
      <p class="brand-subtitle">Jonathan Kohlmeier</p>
      <p class="text-sm text-subtle">
        Writing and advisory for operators who believe craftsmanship still matters. Pipes, RevOps, and
        product systems for people-first teams.
      </p>
      <p class="text-xs text-muted">© {year} Jonathan Kohlmeier. All rights reserved.</p>
    </div>
    <div class="flex flex-col gap-4 text-sm">
      <p class="text-xs uppercase tracking-[0.3em] text-subtle">Stay in the smoke circle</p>
      <p class="text-sm text-subtle">
        Subscribe to the monthly dispatch for experiments, deep dives, and resources for RevOps leaders.
      </p>
      <a
        class="btn-primary btn w-fit"
        href="https://buttondown.email/jonkohlmeier"
        target="_blank"
        rel="noopener noreferrer"
      >
        Join the mailing list
      </a>
    </div>
    <div class="flex flex-col gap-2 text-sm">
      <p class="text-xs uppercase tracking-[0.3em] text-subtle">Elsewhere</p>
      {SOCIAL_LINKS.map((link) => (
        <a
          class="text-subtle hover:text-[var(--color-accent)]"
          href={link.url}
          target={link.url.startsWith('/') ? undefined : '_blank'}
          rel={link.url.startsWith('/') ? undefined : 'noopener noreferrer'}
        >
          {link.label}
        </a>
      ))}
      <a class="text-subtle hover:text-[var(--color-accent)]" href="/now/">Now page</a>
    </div>
  </div>
</footer>
