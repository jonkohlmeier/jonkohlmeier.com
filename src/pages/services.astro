---
import SiteLayout from '../layouts/SiteLayout.astro';
import { getCollection } from 'astro:content';

const recentPosts = (await getCollection('blog'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 2);
---

<SiteLayout title="Advisory & Fractional Support" description="Partner with Jonathan Kohlmeier on RevOps, product, and systems leadership engagements.">
  <section class="mx-auto max-w-4xl space-y-12 text-slate-300">
    <header class="space-y-4 text-center">
      <p class="text-xs uppercase tracking-[0.4em] text-amber-200">Services</p>
      <h1 class="text-4xl font-bold text-slate-100">Fractional leadership for thoughtful teams</h1>
      <p class="text-lg text-slate-300">
        I help growth-minded organizations align revenue operations, product delivery, and customer
        experience—without burning out the humans involved. Engagements are bespoke and always start
        with a diagnostic.
      </p>
    </header>

    <div class="grid gap-8 md:grid-cols-2">
      <div class="rounded-3xl border border-slate-800/70 bg-slate-900/60 p-6 shadow-xl">
        <h2 class="text-xl font-semibold text-slate-100">Fractional RevOps Leadership</h2>
        <p class="mt-3 text-sm">
          Steady hands for your revenue engine. I map systems, untangle data debt, build reporting
          clarity, and guide teams through process changes that stick.
        </p>
        <ul class="mt-4 space-y-3 text-sm">
          <li>• GTM audits & tool consolidation</li>
          <li>• Lifecycle, attribution, and dashboard design</li>
          <li>• Enablement playbooks and ops documentation</li>
        </ul>
      </div>
      <div class="rounded-3xl border border-slate-800/70 bg-slate-900/60 p-6 shadow-xl">
        <h2 class="text-xl font-semibold text-slate-100">Product & Engineering Operations</h2>
        <p class="mt-3 text-sm">
          Need a builder who can bridge strategy and implementation? I facilitate discovery, streamline
          hand-offs, and align product roadmaps with revenue priorities.
        </p>
        <ul class="mt-4 space-y-3 text-sm">
          <li>• Technical due diligence & migration roadmaps</li>
          <li>• Cross-functional rituals and communication frameworks</li>
          <li>• Performance, accessibility, and DX improvements</li>
        </ul>
      </div>
    </div>

    <section class="rounded-3xl border border-amber-300/60 bg-slate-900/60 p-8 shadow-xl">
      <h2 class="text-2xl font-semibold text-amber-200">How an engagement works</h2>
      <ol class="mt-4 space-y-3 text-sm">
        <li><strong>Discovery session.</strong> We unpack goals, stakeholders, and success metrics.</li>
        <li><strong>Diagnostic sprint.</strong> I audit systems, tooling, and team rituals to surface the most impactful opportunities.</li>
        <li><strong>Roadmap & enablement.</strong> We co-create plays, instrumentation, and communication that empower your team to execute.</li>
        <li><strong>Fractional partnership.</strong> Embed me weekly or monthly to ensure outcomes land and momentum sticks.</li>
      </ol>
      <a
        class="mt-6 inline-flex rounded-full bg-amber-300 px-5 py-2 text-sm font-semibold text-slate-900 transition hover:bg-amber-200"
        href="/contact/"
      >
        Request an intro call
      </a>
    </section>

    <section class="space-y-4">
      <h2 class="text-2xl font-semibold text-slate-100">Related writing</h2>
      <div class="grid gap-4 md:grid-cols-2">
        {recentPosts.map((post) => (
          <article class="rounded-2xl border border-slate-800/70 bg-slate-900/60 p-4 shadow-xl">
            <h3 class="text-lg font-semibold text-slate-100">
              <a href={`/blog/${post.id}/`}>{post.data.title}</a>
            </h3>
            <p class="mt-2 text-sm text-slate-400">{post.data.description}</p>
            <a class="mt-3 inline-block text-xs uppercase tracking-[0.3em] text-amber-200" href={`/blog/${post.id}/`}>
              Read essay →
            </a>
          </article>
        ))}
      </div>
    </section>
  </section>
</SiteLayout>
